name = "ca-gelt-deepgram-token-2026"
main = "src/index.js"
compatibility_date = "2026-09-15"
workers_dev = true

# Set ALLOWED_ORIGIN in the Cloudflare dashboard after the public frontend URL exists.
# Keep DEEPGRAM_API_KEY as an encrypted Worker Secret; never put it in this file.
