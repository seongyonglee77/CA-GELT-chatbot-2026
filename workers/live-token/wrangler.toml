name = "ca-gelt-chatbot"
main = "src/index.js"
compatibility_date = "2026-09-15"
workers_dev = true

[assets]
directory = "../../apps/model1_web/static"
binding = "ASSETS"
run_worker_first = true

# DEEPGRAM_API_KEY is already registered as an encrypted Worker Secret.
# Set ALLOWED_ORIGIN to the final public origin before production use.
